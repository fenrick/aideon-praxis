# Temporal and Scenario Context

This document has been decomposed into a folder of small, single-topic files.

**See [temporal-and-scenario/](./temporal-and-scenario/README.md) for the index.**

The `Viewpoint` shape, HLC encoding, layer policy, resolution rules, scenario overlays, diff, explainability, and error codes each now live in their own file under that folder.
