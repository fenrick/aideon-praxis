.github/workflows/reusable-release.yml

pnpm-lock.yaml
cargo.lock

# Generated by Next.js on every build; self-declares "should not be edited".
next-env.d.ts
app/AideonDesignSystem/**
src/design-system/components/**

# Vendored agent-skill definitions — third-party content, not ours to format.
.agents/

# Generated typed IPC client (tauri-specta, ADR-0039); the generator owns its
# shape. Regenerated by the host bindings test; CI fails on any git diff.
src/adapters/ipc-bindings.gen.ts

# ipc-manifest.json is generated from the Rust commands by the host bindings
# test; event/shell-command manifests are still `aideon_xtask` output. The
# generators own their shape.
docs/contracts/ipc-manifest.json
docs/contracts/event-manifest.json
docs/contracts/shell-command-manifest.json

# Rewritten by release-please on every release PR in its own style (e.g. it
# expands `"capabilities": ["default"]` to a multi-line array prettier rejects).
# The tool owns these files' shape; format-checking them just breaks releases.
CHANGELOG.md
src-tauri/tauri.conf.json
