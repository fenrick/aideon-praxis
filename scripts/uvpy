#!/usr/bin/env bash
set -euo pipefail
if ! command -v uv >/dev/null 2>&1; then
  echo "[skip] 'uv' not found; install from https://github.com/astral-sh/uv" >&2
  exit 0
fi
exec uv run --with "packages/worker[dev]" "$@"
